# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# legacy CASC site — reference only (12MB scrape), excluded from the repo.
# The runtime legal HTML was already copied into src/. Reference lives outside git.
/_legacy

# internal tooling / working notes — not part of the project
/.atl
CASC_Prompt_Base_Plataforma.md
*.code-workspace
